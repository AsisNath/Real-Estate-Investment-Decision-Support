fastapi==0.116.1
uvicorn[standard]==0.35.0
jinja2==3.1.6
pydantic==2.11.7
pytest==8.4.1
markdown==3.8.2
# Powers automatic policy research. The app runs without it - research falls
# back to the copy-and-paste prompt - but nothing else needs the network.
anthropic==0.120.2
